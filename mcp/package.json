{
  "name": "@fuzeone/mcp-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "MCP (Model Context Protocol) stdio server scaffold for the FuzeFront platform. enabled:false in .fuze/manifest.json until a working tool is wired. Owned by mcp-maintainer for upkeep; tool behaviour is backend-engineer's.",
  "scripts": {
    "start": "node --experimental-strip-types server.ts"
  }
}
