# Resolve the platform's published packages (@izzywdev/fuzefront-*) from GitHub
# Packages. NOTE: @fuzefront is a workspace-internal scope only and does not exist
# on GitHub — @fuzefront/* is resolved here by npm-workspace symlink / file: path,
# never from a registry. See FuzeSDLC governance/naming-and-addressing.md. Public deps
# still come from the default npm registry. The frontend Docker build context is
# only ./frontend, so this scoped config must live here (the repo-root .npmrc is
# not in the build context). CI/Docker provide GITHUB_TOKEN; for host dev the
# i18n runtime is resolved from local monorepo source via the Vite alias, so a
# token is not required locally.
@izzywdev:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
