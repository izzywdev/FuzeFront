# gitleaks fingerprint allowlist — <commit>:<path>:<rule>:<line>
#
# Entries here suppress a finding for ONE specific occurrence in history. Prefer
# this over adding a value to .gitleaks.toml's `regexes` when the finding is a
# real-but-non-secret identifier: a regex entry would re-commit the very string
# we are trying to keep out of the repo, permanently and in plaintext.
#
# ---------------------------------------------------------------------------
# packages/feature-flags/tests/unleash-provider.test.ts
# A test fixture hard-coded the platform owner's `users.id` UUID while proving
# the targetingKey -> Unleash `userId` mapping that the `developers` segment
# constrains. gitleaks scores a UUID as a high-entropy generic-api-key.
#
# It is NOT a credential — it is a user identifier, and it grants nothing on its
# own (flags gate visibility; authorization is enforced by Permit server-side,
# and /api/flags derives the user from the authenticated session, never from
# client input). It was replaced with a synthetic UUID in a later commit, but it
# remains in this commit's history and history is not rewritten on this repo.
6c21dfd3e67158b0059b1657eb7f65b722dc50a0:packages/feature-flags/tests/unleash-provider.test.ts:generic-api-key:13
# deploy/contabo/sealed/fuzefront-secrets.yaml — CONSUMER_REGISTRATION_SECRET is a
# kubeseal-encrypted ciphertext bound to the fuzefront-secrets SealedSecret in the
# fuzefront namespace. It is asymmetrically encrypted with the cluster's public key
# and cannot be decrypted outside the cluster. Safe to commit (SealedSecrets design intent).
53fa34b2d69b1508748dc6bd9c3caec969438ad8:deploy/contabo/sealed/fuzefront-secrets.yaml:generic-api-key:16
# services/selection-list-service/tests/resolve.test.ts (commit 010a995 — S8 resolve hot path).
# A unit-test JWT signing key variable used only inside the test file to sign JWTs for
# supertest. Not a production secret; it was renamed to a non-flagging identifier in a
# later commit on the S8 branch. History is not rewritten on this repo.
010a995cebabeba3bb38c47da16326f8fe218658:services/selection-list-service/tests/resolve.test.ts:generic-api-key:71
# The prior .gitleaksignore commit (cae9e719) had the flagged variable value quoted in its
# comment text. Suppressing that commit's finding without re-quoting the value.
cae9e719d90e7d3c278a89576500e04ac4b25c3d:.gitleaksignore:generic-api-key:27
