{{- if .Values.configService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fuzefront-config-service
  labels:
    {{- include "fuzefront.labels" . | nindent 4 }}
    app.kubernetes.io/component: config-service
spec:
  replicas: {{ .Values.configService.replicas }}
  selector:
    matchLabels:
      app: fuzefront-config-service
  template:
    metadata:
      labels:
        app: fuzefront-config-service
        {{- include "fuzefront.labels" . | nindent 8 }}
    spec:
      {{- include "fuzefront.scheduling" (dict "svc" .Values.configService "root" .) | nindent 6 }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: config-service
          image: "{{ .Values.configService.image.repository }}:{{ .Values.configService.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.configService.port }}
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: {{ .Values.configService.port | quote }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: config-service-secrets
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: config-service-secrets
                  key: JWT_SECRET
            - name: PERMIT_API_KEY
              valueFrom:
                secretKeyRef:
                  name: config-service-secrets
                  key: PERMIT_API_KEY
            - name: PERMIT_PDP_URL
              value: {{ .Values.configService.permitPdpUrl | quote }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.configService.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.configService.port }}
            initialDelaySeconds: 30
            periodSeconds: 15
          resources:
            {{- toYaml .Values.configService.resources | nindent 12 }}
{{- end }}
