name: A2A Maintain

# Keeps this repo's agent-to-agent (A2A) surface current, as part of CI. On every PR it
# runs the `a2a-maintainer` agent to check whether anything changed that requires the
# repo's A2A onboarding to be BUILT (first time) or UPDATED (drift) — the manifest a2a
# block + providesTo, the serving role(s) and their card projection, and (platform repo)
# the tenant registration. If so, it commits the change BACK TO THE PR BRANCH; when it
# can't (fork PR / first-time build), it opens a separate `auto-merge`-labelled follow-up
# PR. It never fabricates a role's product behaviour, never handles creds, never deploys.
#
# LLM credential: routed through .github/actions/llm-endpoint (LiteLLM in-cluster with
# automatic fallback to a direct vendor — see that action for the design). Requires repo
# secret LITELLM_FUZE_KEY and/or ANTHROPIC_API_KEY (at least one).
#
# CONTRACT: it used to be "no key -> SKIPS (never red)". That made a check which passes
# by not running — a repo declaring an A2A surface could go indefinitely with that
# surface unmaintained while the check sat green. The rule now keys off the MANIFEST,
# because that is what distinguishes an absence from a gap:
#
#   a2a.enabled not declared  -> skip. There is no surface; that is the truth.
#   a2a.enabled true, no usable credential (neither LiteLLM nor the fallback vendor)
#                              -> FAIL. A declared surface nobody can maintain.
#   a2a.enabled true, credential available (LiteLLM OR fallback) -> run.
#
# The `a2a-maintainer` agent def is stamped by sdlc-bootstrap into .claude/agents/ and
# reconciled by governance-sync; this workflow just invokes it.

on:
  pull_request:

permissions:
  contents: write          # commit the reconciled A2A surface back to the PR branch
  pull-requests: write      # open a follow-up PR when a push-back isn't possible
  id-token: write          # claude-code-action fetches an OIDC token (required)
  actions: read

concurrency:
  group: a2a-maintain-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  # Runs on a HOSTED runner and always reports. Split out from the maintainer on
  # purpose: a job on a self-hosted pool that is not serving QUEUES FOREVER with no
  # red X, no log and no timeout (measured against this fleet: 175+ jobs queued on
  # `fuzesdlc` with zero running for 36h, FuzeInfra#623). A misconfiguration that
  # produces no signal at all is worse than one that produces a red, so the check
  # that must never be silent runs where it cannot be silenced — and per the
  # llm-endpoint fallback design, the maintainer job below no longer NEEDS a
  # self-hosted/in-cluster runner at all, so this split is now purely about signal
  # placement, not reachability.
  preflight:
    name: a2a-maintain preflight
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      declared: ${{ steps.check.outputs.declared }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2

      - name: Is a2a-maintain declared?
        id: check
        shell: bash
        run: |
          set -uo pipefail
          declared=$(python3 -c "import json;print(str((json.load(open('.fuze/manifest.json')).get('a2a') or {}).get('enabled', False)).lower())" 2>/dev/null || echo false)
          echo "declared=$declared" >> "$GITHUB_OUTPUT"
          if [ "$declared" != "true" ]; then
            echo "::notice title=a2a-maintain::.fuze/manifest.json does not declare a2a.enabled — nothing to maintain. Skipping is correct here: there is no surface."
          fi

      # Declared. From here, having NO usable credential (neither LiteLLM nor the
      # configured fallback vendor) is a MISCONFIGURATION, not an environmental gap,
      # and must fail — that is the whole point of keying this off the manifest
      # instead of "no key -> skip". This step's own ::error:: names the exact
      # remedy when it fails, so the job doesn't need to duplicate that message.
      - name: Resolve LLM endpoint — proves a usable credential exists
        if: steps.check.outputs.declared == 'true'
        uses: ./.github/actions/llm-endpoint
        with:
          litellm-key: ${{ secrets.LITELLM_FUZE_KEY }}
          fallback-vendor: ${{ vars.FUZE_LLM_FALLBACK_VENDOR || 'anthropic' }}
          fallback-anthropic-key: ${{ secrets.ANTHROPIC_API_KEY }}

  a2a-maintain:
    needs: preflight
    # Two conditions, one key (a duplicate `if:` is a YAML syntax error, not an AND):
    #   - preflight said the surface is declared AND a credential is usable (preflight
    #     job fails outright otherwise, so `needs.preflight` never reaches here with a
    #     declared surface and no credential).
    #   - not our own follow-up branch, or the maintainer re-triggers on its own push.
    if: >-
      needs.preflight.outputs.declared == 'true' &&
      !startsWith(github.event.pull_request.head.ref, 'a2a-maintain/')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR head (writable, same-repo only)
        # Pinned to a full commit SHA, not a mutable tag: a tag can be silently
        # repointed by the action owner (cf. the trivy-action / kics-github-action
        # compromises). This workflow has `contents: write` on the PR branch, so a
        # repointed checkout would run attacker code with push access.
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name }}
          fetch-depth: 0

      - name: Resolve LLM endpoint (LiteLLM in-cluster, vendor fallback)
        id: llm
        uses: ./.github/actions/llm-endpoint
        with:
          litellm-key: ${{ secrets.LITELLM_FUZE_KEY }}
          fallback-vendor: ${{ vars.FUZE_LLM_FALLBACK_VENDOR || 'anthropic' }}
          fallback-anthropic-key: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Skip on last-commit marker
        id: marker
        run: |
          if git log -1 --pretty=%B | grep -q '\[skip a2a\]'; then
            echo "::notice::last commit marked [skip a2a] — skipping to avoid a self-trigger loop."
            echo "run=false" >> "$GITHUB_OUTPUT"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Run a2a-maintainer
        if: steps.marker.outputs.run == 'true'
        uses: anthropics/claude-code-action@428971d2ecd6e3a7cb0ee0da2a3a8b33fdb3678d  # v1
        env:
          ANTHROPIC_BASE_URL: ${{ steps.llm.outputs.base-url }}
          ANTHROPIC_AUTH_TOKEN: ${{ steps.llm.outputs.auth-token }}
        with:
          anthropic_api_key: ${{ steps.llm.outputs.auth-token }}
          # Every FuzeFront agent PR is authored by the `claude` bot; dependabot and
          # github-actions[bot] (governance-sync commit-back) also open/push PRs. Without
          # this, claude-code-action's actor check hard-fails those runs with
          # "Workflow initiated by non-human actor ... Add bot to allowed_bots list or use
          # '*'", turning a2a-maintain red on every bot-authored PR. Allow all bots so the
          # A2A upkeep stream runs (or no-ops) instead of blocking the PR.
          allowed_bots: "*"
          prompt: |
            You are the **a2a-maintainer** for this repo (follow `.claude/agents/a2a-maintainer.md`
            exactly — same scope, boundaries, and done-contract). This is an automated CI run on
            PR #${{ github.event.pull_request.number }} (head `${{ github.event.pull_request.head.ref }}`).

            TASK: assess whether this PR changed anything that requires the repo's A2A surface to be
            built (first time) or updated (drift) — the `.fuze/manifest.json` a2a block + `providesTo`,
            the serving role(s) under `agent-templates/roles/` and their card projection against the
            frozen A2A contract, contract-version currency, and (platform repo only) the shared-server
            `tenants` registration. If the surface is already in sync, DO NOTHING and say so.

            IF a change is needed:
              - Make the minimal correct change (scaffold on first run; reconcile only what drifted).
              - COMMIT IT BACK TO THIS PR BRANCH: `chore(a2a-maintain): <what> [skip a2a]` and push to
                `${{ github.event.pull_request.head.ref }}`. The trailing `[skip a2a]` is REQUIRED so you
                do not re-trigger yourself.
              - If you cannot push to the PR branch (this is a fork PR), instead open a follow-up PR from
                an `a2a-maintain/pr-${{ github.event.pull_request.number }}` branch, add the `auto-merge`
                label, and comment the link on this PR.

            HARD BOUNDARIES: never fabricate a role's product/domain behaviour (scaffold a card-valid
            skeleton and TODO-flag the real behaviour), never handle secrets/creds or run `register-a2a-cli`,
            never `kubectl`/deploy, never flip a tenant/role to `enabled: true` whose card can't yet project,
            never edit the frozen `contracts/a2a/v1/**`. Verify any card you touch projects schema-valid.

            End with the done-contract line from your agent def
            (`A2A SURFACE: <in-sync|scaffolded|reconciled>` + what landed + `NEEDS PRODUCT/OPERATOR:` if any).
          claude_args: '--allowedTools "Bash,Read,Edit,MultiEdit,Write,Glob,Grep,WebFetch"'
