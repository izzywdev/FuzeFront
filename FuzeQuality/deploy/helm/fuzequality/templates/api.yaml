apiVersion: apps/v1
kind: Deployment
metadata:
  name: fuzequality-backend
  labels:
    {{- include "fuzequality.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.backend.replicas }}
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 0, maxUnavailable: 1 }
  selector: { matchLabels: { app.kubernetes.io/name: fuzequality, app.kubernetes.io/component: backend } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: fuzequality, app.kubernetes.io/component: backend }
      annotations: { prometheus.io/scrape: "true", prometheus.io/port: {{ .Values.backend.port | quote }}, prometheus.io/path: /metrics }
    spec:
      securityContext: { fsGroup: 1000, fsGroupChangePolicy: OnRootMismatch }
      volumes:
        - name: runtime-tmp
          emptyDir: { sizeLimit: 64Mi }
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: backend
          image: "{{ .Values.backendImage.repository }}:{{ .Values.backendImage.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          command: ["npm", "run", "start:api"]
          ports: [{ name: http, containerPort: {{ .Values.backend.port }} }]
          env:
            {{- include "fuzequality.env" . | nindent 12 }}
            # Where docker/Dockerfile put the contract. The image already sets
            # this as an ENV; naming it here too means the path is visible in
            # `helm template` and in `kubectl describe pod`, which is where
            # someone debugging a 503 from /openapi.yaml will actually look.
            # The contract is baked into the image, not mounted from a
            # ConfigMap, so what this deployment serves is the spec it was
            # built from and cannot drift from the running code.
            - name: OPENAPI_SPEC_PATH
              value: /workspace/FuzeQuality/contracts/openapi.yaml
          envFrom: []
          volumeMounts:
            - { name: runtime-tmp, mountPath: /tmp }
          readinessProbe: { httpGet: { path: /health/ready, port: http }, initialDelaySeconds: 5 }
          livenessProbe: { httpGet: { path: /health/live, port: http }, initialDelaySeconds: 15 }
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
          securityContext: { allowPrivilegeEscalation: false, readOnlyRootFilesystem: true, runAsNonRoot: true, runAsUser: 1000, runAsGroup: 1000 }
---
apiVersion: v1
kind: Service
metadata:
  name: fuzequality-backend
  labels:
    {{- include "fuzequality.labels" . | nindent 4 }}
spec:
  selector: { app.kubernetes.io/name: fuzequality, app.kubernetes.io/component: backend }
  ports: [{ name: http, port: {{ .Values.backend.port }}, targetPort: http }]
